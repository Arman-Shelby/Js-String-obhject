const first = 'Abid';
const last = 'Nabid';

// const fullName = first + last;
const fullName = first + ' ' + last;
// console.log(fullName);
const fullName2 = first.concat(' ').concat(last);
console.log(fullName2);

//includes
console.log(last.includes('d')); //strings works like array sometimes while concat or includes etc